wheel==0.29.0 # necessary to package, not to run
twine==1.8.1 # necessary to publish, not to run
Cython>=0.25.2 # necessary to build, not to run
numpy>=1.12.0
scipy>=0.19.0
versioneer>=0.18 # necessary for project bootstrap/versioning code update only
